#include <stdio.h>
#include <stdlib.h>

int main()
{

    //Exe 1


    int num;
    printf("Informe um valor para calculo da tabuada: ");
    scanf("%d", &num);

    for(int i = 0; 1 <=10; i++)
        printf("\t%d * %d = %d\n", num, i, num*1);





























    return 0;
}
